import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#b392ac",
      contrastText: "#edf2f4",
    },
    secondary: {
      main: "#14213d",
      contrastText: "#edf2f4",
    },
  },
});
