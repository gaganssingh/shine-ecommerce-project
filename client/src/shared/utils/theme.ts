import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#fe6d73",
      contrastText: "#edf2f4",
    },
    secondary: {
      main: "#14213d",
      contrastText: "#edf2f4",
    },
  },
});
